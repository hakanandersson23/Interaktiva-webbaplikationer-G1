@model ProjektGruppF.ViewModels.Freelancer

@{
    ViewBag.Title = "MainAbilitiesL";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>MainAbilitiesL</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Firstname)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.MainAbilitiesList)
    {
        @item.name
        
    }
    <tr>
        <td>
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id = Model. }) |
            @Html.ActionLink("Details", "Details", new { id = item.main_abilities_id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.main_abilities_id })*@
        </td>
    </tr>
    }

</table>
